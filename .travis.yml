language: go
go:
  - 1.9

sudo: required
before_install:
  - export GOPATH="${TRAVIS_BUILD_DIR}/Godeps/_workspace:$GOPATH"
  - export PATH="${TRAVIS_BUILD_DIR}/Godeps/_workspace/bin:$PATH"
  - bash -x files/check-install.sh

script:
  - bash -x files/check.sh

notifications:
  email: false
